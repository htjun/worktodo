{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "worktodo",
  "title": "Worktodo",
  "description": "A local-first personal task manager for Raycast.",
  "private": true,
  "author": "jsn",
  "license": "MIT",
  "icon": "worktodo-icon.png",
  "platforms": [
    "macOS"
  ],
  "categories": [
    "Productivity"
  ],
  "engines": {
    "node": ">=22.22.2 <25"
  },
  "packageManager": "npm@11.16.0",
  "scripts": {
    "dev": "ray develop",
    "dev:mcp": "npm run build:mcp && node dist/mcp/server.js",
    "build": "npm run build:raycast",
    "build:raycast": "ray build",
    "build:mcp": "tsc -p tsconfig.mcp.json",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.mcp.json",
    "lint": "CI=true ray lint && eslint mcp tests",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "test": "npm run build:mcp && vitest run",
    "verify": "npm run format && npm run lint && npm run typecheck && npm test && npm run build && npm run build:mcp",
    "publish": "npx @raycast/api@latest publish",
    "prepublishOnly": "node -e \"throw new Error('Use npm run publish for the Raycast Store')\""
  },
  "commands": [
    {
      "name": "my-tasks",
      "title": "All Tasks",
      "subtitle": "Worktodo",
      "description": "View all Worktodo tasks",
      "mode": "view"
    },
    {
      "name": "quick-add",
      "title": "Quick Add",
      "subtitle": "Worktodo",
      "description": "Create a task with optional details",
      "mode": "view"
    },
    {
      "name": "backup-restore",
      "title": "Backup & Restore",
      "subtitle": "Worktodo",
      "description": "Export and restore Worktodo JSON backups",
      "mode": "view"
    },
    {
      "name": "menu-bar",
      "title": "Worktodo Menu Bar",
      "subtitle": "Worktodo",
      "description": "View and complete tasks due this week from the menu bar",
      "mode": "menu-bar",
      "interval": "1m"
    }
  ],
  "dependencies": {
    "@raycast/api": "2.4.1"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@modelcontextprotocol/server": "2.0.0",
    "@raycast/eslint-config": "2.2.0",
    "@types/node": "22.19.17",
    "@types/react": "19.0.10",
    "eslint": "10.9.0",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "vitest": "4.1.11",
    "zod": "4.4.3"
  }
}
